# Разные параметры окружения для разных тестов

## Запуск

Надо отлаживать тк возможны проблемы с волумами

Из минусов - надо прокидывать IMAGEVERSION-ы через пару контейнеров - можно подумать как их дампнуть в env файл и потом везде применять оттуда (как в фото/ори частично сделано)  

```shell
dc run --rm all
```

```shell
docker-compose exec e2e python3 -m vedro run
```
